import { SiteConfig } from "@/types"

export const siteConfig: SiteConfig = {
  name: "Proposal Up",
  description:
    "An application that uses context of the user and ChatGPT to generate Upwork Proposals for job postings.",
  url: "https://proposal-up.com",
  links: {
    airi: "https://airi-labs.com/"
  }
}